name: Tags

on:
  create:
    tags:
      - '*'

jobs:
  lint:
    uses: ./.github/workflows/lint.yml

  build:
    needs: lint
    uses: ./.github/workflows/build.yml

  deploy-prod:
    needs: lint
    uses: ./.github/workflows/deploy-prod.yml
    secrets: inherit

