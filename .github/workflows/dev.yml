name: Deploy DEV

on:
  push:
    branches:
      - dev

jobs:
  check:
    uses: ./.github/workflows/check.yml

  build-dev:
    needs: check
    uses: ./.github/workflows/build.yml

  deploy-dev:
    needs: build-dev
    uses: ./.github/workflows/deploy-dev.yml
    secrets: inherit

