name: Deploy PROD

on:
  push:
    branches:
      - master

jobs:
#  lint:
#    uses: ./.github/workflows/lint.yml

  release:
#    needs: lint
    uses: ./.github/workflows/release.yml
    secrets: inherit

