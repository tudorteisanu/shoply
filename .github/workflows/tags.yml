name: Tags

on:
  create:
    tags:
      - '*'

jobs:
  check:
    uses: ./.github/workflows/check.yml

  build:
    needs: check
    uses: ./.github/workflows/build.yml

  deploy:
    needs: build
    uses: ./.github/workflows/deploy-prod.yml
    secrets: inherit

