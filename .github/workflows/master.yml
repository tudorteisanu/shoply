name: Deploy PROD

on:
  push:
    branches:
      - master

jobs:
#  check:
#    uses: ./.github/workflows/check.yml

  release:
#    needs: check
    uses: ./.github/workflows/release.yml
    secrets: inherit

